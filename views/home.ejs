<%- include("partials/header") -%>

<h1><%= blog1.title %></h1>
<p><%= blog1.content %></p>

<a href="/compose"><button type="submit" class="btn btn-success" id="compose-button">➕ Compose New</button></a>

<% blogList.forEach(blog => { %>
<h1><%= blog.title %></h1>
<p>
  <%= blog.content.substring(0,100) + "... " %>
  <a href="/posts/<%= blog._id %>">Read More</a>
</p>

<% }) %>

<%- include("partials/footer") -%>
